/* * This Code is heavily influenced by the NodeBox Colors Library.  * See http://nodebox.net/code/index.php/Colors and http://nodebox.net *  * Ported and rewritten in 2009 by Philipp Laeubli and licensed under GPL.  * See LICENSE.txt for details. *  * Copyright (c) 2009 Philipp Laeubli * Original NodeBox Colors Library: Tom De Smedt, Frederic De Bleser * */package ch.badmojo.color {	import ch.badmojo.color.tutorial.ColorSprite;		import flash.display.Sprite;		/**	 * This class is made for the ColorTutorial 001. It shows how you can use	 * the color properties to create DisplayObjects that use the Color object.	 * @author phil	 */	public class ColorTutorial001 extends Sprite {		public function ColorTutorial001() {						// create a red color			var color : Color = new Color(142, 27, 19);						// create a ColorWheel with ten warm variations of that color; Make			// those colors darker, so that we can brighten them up later with  			// mouse events.			var wheel : ColorWheel = color.toWarm(10);			wheel.darken(20);			wheel.deSaturate(20);						// loop through the colors and create a ColorSprite for each color.			for (var i : int = 0;i < wheel.length(); i++) {				var sprite : Sprite = new ColorSprite(wheel.getColor());				sprite.y = this.height;				addChild(sprite);				}		}	}}